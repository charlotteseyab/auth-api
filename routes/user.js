import express from "express";
const router = express.Router();

// //Middlewares
// import { authenticated, requireSignin } from "../middlewares";
// //Controllers
// const { changePassword, updateProfile } = require("../controllers/user");

// router.post("/password/change", authenticated, changePassword);
// router.put("/profile/update", authenticated, updateProfile);

export default router;
